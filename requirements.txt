# Everything needed to actually run the pipeline locally:
#   pip install -r requirements.txt
#   python -m pipeline.run
#   python -m pipeline.lookup ORDER-00001
#
# No external services required -- DuckDB and SQLite are both embedded.
# apache-airflow is intentionally NOT here: see requirements-airflow.txt.

duckdb>=1.0
pandas>=2.0
pyyaml>=6.0
faker>=20.0

# Testing
pytest>=7.0
